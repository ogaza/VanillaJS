import Router from "./services/router.js";

window.addEventListener("DOMContentLoaded", startTheApp);

function startTheApp() {
  console.log(
    "this is the simple app using just the vanilla js - so just the javascript without any framework on top of that"
  );
}
