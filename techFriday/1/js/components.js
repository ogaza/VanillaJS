export function homeComponent(parentNode) {
  parentNode.textContent = "home view";
}

export function aboutMeComponent(parentNode) {
  parentNode.textContent = "about me view";
}

export function pageNotFoundComponent(parentNode) {
  parentNode.textContent = "page not found";
}
